# 1. 克隆仓库
git clone https://github.com/jiyongdian/jimeng.git
cd jimeng

# 2. 创建 Dockerfile（复制下面的内容）
# 3. 推送到 GitHub
git add Dockerfile
git commit -m "Add Dockerfile with Playwright dependencies"
git push origin main

